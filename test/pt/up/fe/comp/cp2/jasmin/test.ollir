import test.Exemplo;

HelloWorld {

    .method public foo(a.Exemplo).i32 {

        e.Exemplo :=.Exemplo new(Exemplo).Exemplo;
        invokespecial(e.Exemplo, "<init>").V;

        res.i32 :=.i32 invokevirtual(e.Exemplo, "getFunction").i32;

        ret.i32 res.i32;

    }

    .construct HelloWorld().V {
        invokespecial(this, "<init>").V;
    }
}